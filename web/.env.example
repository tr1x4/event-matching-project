# Локальный `npm run dev` (Vite читает .env.development, см. web/.env.development).
# Для Docker-сборки фронта используйте корневой `.env` (см. `/.env.example` в корне репозитория).

VITE_AUTH_URL=http://127.0.0.1:8003
VITE_PROFILES_URL=http://127.0.0.1:8001
VITE_MATCH_URL=http://127.0.0.1:8000
VITE_EVENTS_URL=http://127.0.0.1:8002
VITE_CHATS_URL=http://127.0.0.1:8004
